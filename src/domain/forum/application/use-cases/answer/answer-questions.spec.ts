import { test, expect } from 'vitest'
import { AnswerQuestionsUseCase } from './answer-questions';

test("create an answer question use case", () => {
  // const answerQuestionsUseCase = new AnswerQuestionsUseCase();

  // const result = await answerQuestionsUseCase.execute({ instrutorId: "1", questionId: "2", content: "content" });

  // expect(result.isRight()).toBeTruthy();
  // expect(result.value?.answer.content).toEqual("content");
})